# MoodLite 轻心记 - V2.0 重构版产品规划书 (PRD)

## 模块一：产品概述与技术基座定义 (架构层)

- **产品定位**：一款专注情绪追踪与自我觉察的轻量级日记应用，主打低门槛记录与数据可视化。
- **开发框架**：HarmonyOS ArkUI (API 9+)，全面采用 ArkTS 声明式语法。
- **重构核心目标**：
  - **解耦**：彻底分离 UI 渲染层与数据逻辑层。
  - **性能**：优化长列表（懒加载）与图表渲染的内存占用。
  - **统一**：建立严格的全局设计系统 (Design System)，避免组件样式碎片化。

## 模块二：全局视觉规范 (UI/UX 骨架层)

为确保视觉的统一性，所有前端页面必须强制继承以下规范：

- **色彩系统**：
  - 全局底色：浅灰白 (`#F7F8FA`)
  - 品牌主色 / 愉悦情绪：蜜桃粉 (`#FF8BA7`)
  - 低落情绪辅色：雾霾蓝 (`#AEC6CF`)
  - 文本阶梯：深灰主文本 (`#1F2937`)，浅灰次文本 (`#9CA3AF`)
- **卡片设计规范 (Glassmorphism 玻璃拟态)**：
  - **核心约束**：采用轻量级毛玻璃质感。**严格禁止使用全包围式的浓重发光边缘，禁止使用高亮刺眼的粗边框。**
  - **具体参数**：纯白背景 (`#FFFFFF`)，大圆角 (`.borderRadius(24)`)，配合极弱的下沉弥散阴影（如：`radius: 24, color: 'rgba(0, 0, 0, 0.03)', offsetY: 8`）。
- **空间与布局**：
  - 根容器顶部固定采用高度 35% 的线性渐变呼吸层（从 `#FFE5EC` 渐变至透明）。
  - 卡片间距强制采用 16vp 或 24vp，杜绝随意设定的边距。

## 模块三：核心数据模型 (数据层)

在重构初期，必须优先建立坚固的数据模型，作为所有页面的单一事实来源 (Single Source of Truth)：

1. **基础数据表 (MoodRecord)**

- `id` (String): UUID，主键。
- `timestamp` (Number): 核心排序依据。
- `dateStr` (String): 格式化日期 (YYYY-MM-DD)，用于日历聚合与热力图计算。
- `score` (Number): 情绪净值 (-2 到 2 的整数)。
- `text` (String): 用户文本输入。
- `images` (Array\<String>): 本地沙盒路径占位符集合。
- `location` (String): 结构化位置信息。

1. **统计视图模型 (ViewModel)**

- `CalendarDayItem`: 映射至热力图网格的单日聚合数据。
- `MonthlyStats`: 映射至概览卡片的月度汇总数据（包含占比计算）。

## 模块四：核心页面结构规划 (表现层)

本部分聚焦页面的结构占位，便于快速搭建 ArkUI 骨架：

### 页面 A：全局导航 (MainPage)

- **结构**：底部悬浮式 TabBar + 顶部无滚动 TabContent。
- **组件要求**：胶囊状外观，应用全局轻量卡片阴影，选中状态高亮为品牌粉色。

### 页面 B：欢迎与引导 (Welcome)

- **结构划分**：
  - 顶部占位：主副标题横向排布。
  - 核心区占位：垂直居中的巨型卡片（包含大图占位、欢迎词、操作按钮）。
  - 浮动区占位：右下角绝对定位的 FAB (悬浮操作按钮)。

### 页面 C：记录交互 (AddEntry)

- **结构划分**：
  - 顶导占位：返回 + 标题 + 完成。
  - 交互区占位：3D 表情图占位 + 渐变 Slider 滑块组件。
  - 输入区占位：无边框 TextArea + 底部固定三个正方形工具方块横向均分。

### 页面 D：数据可视化 (Stats)

- **结构划分**：
  - 热力图占位：顶部月份控制器 + 7列星期表头 + 7x5 Grid 循环渲染区。
  - 概览区占位：Row 均分包裹 4 个 Column 数据块，每个数据块底部附带情绪色极细装饰线。

## 模块五：工程实施路径 (Roadmap)

1. **Phase 1 (基础搭建)**：搭建路由框架、定义全局卡片 `@Styles`、完成 `MoodRecord` 模型定义。
2. **Phase 2 (核心闭环)**：实现 AddEntry 记录页的数据写入与 Timeline 列表的读取。
3. **Phase 3 (算法与视图)**：实现 Stats 页面的月份数据聚合算法与热力图 UI 渲染。
4. **Phase 4 (润色与高阶)**：加入列表滑动删除动效、组件进场 `animateTo` 动画、以及桌面小组件。

